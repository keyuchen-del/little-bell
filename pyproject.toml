[project]
name = "little-bell"
version = "0.1.0"
description = "小铃铛 - Agent 等待通知推送工具"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pillow>=12.2.0",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
    "rumps>=0.4.0",
]
